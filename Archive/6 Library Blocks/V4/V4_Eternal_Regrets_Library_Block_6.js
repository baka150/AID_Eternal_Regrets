// start JavaScript Code Segment 6 of 36, original lines 51-60 (continued - unbalanced braces)
let HOOK = inHook;
    let TEXT = inText;
    let STOP = inStop;
// end Library Block 3/16
// start Library Block 4A/16
let AC = state.AutoCards || {};
if (!AC.config) {
    AC.config = {
        doAC: DEFAULT_DO_AC,
        pinConfigureCard: DEFAULT_PIN_CONFIGURE_CARD,
// end JavaScript Code Segment 6 of 36